#!/usr/bin/env bash

function main() {
  while read line; do
    if [ "${line:0:1}" = '#' ]; then
      continue  # comments are ignored
    fi
    export $line > /dev/null
  done < "$1"
  echo "Loaded $1 to shell"
}

main "$@"